#include<bits/stdc++.h>
using namespace std;

int main()
{
    int m;
    cin>>m;
   vector<int> nums;
   for(int i=0;i<m;i++){
     int q;
     cin>>q;
     nums.push_back(q);
   }
    int n= nums.size();
int x=0;
for(int i=0;i<n-1;i++)
{
for(int j=i+1;j<n;j++)
{
if(nums[i]==nums[j])
{
x=nums[i];
}
}
}
cout<<"The duplicate is : "<<x;
  return 0;
}
