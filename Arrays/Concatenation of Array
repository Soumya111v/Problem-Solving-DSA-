#include<bits/stdc++.h>
using namespace std;
int main()
{
  vector<int> nums;
  nums.push_back(10);
  nums.push_back(11);
  nums.push_back(12);
  nums.push_back(13);
  nums.push_back(14);
  nums.push_back(15);
  int n = nums.size();
  for(int i=0;i<n;i++)
  {
    nums.push_back(nums[i]);
  }
  for(auto it=nums.begin();it!=nums.end();it++){
  cout<<*(it)<<" ";
  }
  return 0;
}
