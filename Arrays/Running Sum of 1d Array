#include<bits/stdc++.h>
using namespace std;

int main()
{
   vector<int> nums={10,23,36,6,42,80};
    int sum =0;
    vector<int>v1;
    for(int i=0; i<nums.size();i++)
     {
       sum=sum+nums[i];
       a.push_back(sum);
     }
       for(auto it=a.begin();it!=a.end();it++){
       cout<<*it<<" ";  
     } 
  return 0;
}
